{
  "name": "complete_reminders",
  "description": "Complete or uncomplete one or more reminders by stable IDs.",
  "entrypoint": "run.py",
  "parameters": {
    "reminder_ids": {
      "type": "string",
      "description": "JSON array string or comma/newline-separated reminder IDs."
    },
    "completed": {
      "type": "boolean",
      "description": "Set to false to mark reminders incomplete instead of complete."
    },
    "dry_run": {
      "type": "boolean",
      "description": "Preview completion when completed is true."
    }
  }
}
