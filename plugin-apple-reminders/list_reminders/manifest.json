{
  "name": "list_reminders",
  "description": "List reminders by remindctl-style filter and optional list target.",
  "entrypoint": "run.py",
  "parameters": {
    "filter": {
      "type": "string",
      "description": "Optional filter: today, tomorrow, week, overdue, upcoming, open, completed, all, or date."
    },
    "date": {
      "type": "string",
      "description": "Date value required when filter is date."
    },
    "list": {
      "type": "string",
      "description": "Optional reminder list title/name."
    },
    "list_id": {
      "type": "string",
      "description": "Optional reminder list ID."
    }
  }
}
